$kind: http-request
description: "Confirm Delivery Challan"
url: "{{baseUrl}}/challans/{{challanId}}/confirm"
method: PATCH
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 200", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 5000
