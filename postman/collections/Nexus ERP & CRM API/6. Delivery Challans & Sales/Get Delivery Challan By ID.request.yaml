$kind: http-request
description: "Get Delivery Challan By ID"
url: "{{baseUrl}}/challans/{{challanId}}"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 200", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 3000
