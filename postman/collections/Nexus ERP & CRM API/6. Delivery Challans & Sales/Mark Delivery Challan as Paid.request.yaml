$kind: http-request
description: "Mark Delivery Challan as Paid"
url: "{{baseUrl}}/challans/{{challanId}}/paid"
method: PATCH
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 200", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 6000
