$kind: http-request
description: "Get Customer Challans"
url: "{{baseUrl}}/customers/{{customerId}}/challans"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 200", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 7000
