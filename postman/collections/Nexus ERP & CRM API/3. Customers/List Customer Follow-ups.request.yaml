$kind: http-request
description: "List Customer Follow-ups"
url: "{{baseUrl}}/customers/{{customerId}}/followups"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 200", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 6000
